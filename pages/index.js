import Layout from '../components/Layout';
import MeetupList from '../components/MeetupList'

const Home = () => {
  return (
    <Layout>
      <MeetupList />
    </Layout>
  )
};

export default Home;
